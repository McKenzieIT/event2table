# 游戏管理按钮 - 视觉设计指南

## 按钮外观

### 桌面端布局

```
┌─────────────────────────────────────────────────┐
│                  选择游戏                      × │
├─────────────────────────────────────────────────┤
│  🔍 搜索游戏名称或GID...                    ×  │
├─────────────────────────────────────────────────┤
│  🎮  游戏A                        GID: 10000147 > │
│  🎮  游戏B                        GID: 10000148 > │
│  🎮  游戏C                        GID: 10000149 > │
├─────────────────────────────────────────────────┤
│  共 3 个游戏              [⚙️ 游戏管理]       │
└─────────────────────────────────────────────────┘
```

### 按钮细节

**正常状态**：
```
┌─────────────────┐
│  ⚙️  游戏管理    │
└─────────────────┘
- 背景：白色 (#ffffff)
- 边框：浅灰色 (#e2e8f0)
- 文字：深灰色 (#1e293b)
- 圆角：8px
- 内边距：8px 16px
```

**Hover 状态**：
```
┌─────────────────┐
│  ⚙️  游戏管理    │  ↑ (向上移动 1px)
└─────────────────┘
- 背景：白色
- 边框：蓝色 (#3b82f6)
- 文字：蓝色 (#3b82f6)
- 阴影：0 2px 8px rgba(59, 130, 246, 0.15)
```

**Active 状态**：
```
┌─────────────────┐
│  ⚙️  游戏管理    │  (回到原位)
└─────────────────┘
- 阴影：0 1px 4px rgba(59, 130, 246, 0.1)
```

### 移动端布局

```
┌─────────────────────────────┐
│        选择游戏           × │
├─────────────────────────────┤
│  🔍 搜索游戏名称或GID...   │
├─────────────────────────────┤
│  🎮  游戏A       GID:...   │
│  🎮  游戏B       GID:...   │
│  🎮  游戏C       GID:...   │
├─────────────────────────────┤
│       共 3 个游戏          │
│  [  ⚙️  游戏管理  ]        │
└─────────────────────────────┘
```

**移动端按钮**：
- 宽度：100%（占据整行）
- 居中对齐
- 垂直排列（游戏计数在上，按钮在下）

## CSS 类名

### 主容器
- `.game-management-btn` - 按钮主类

### 状态类
- `:hover` - 鼠标悬停状态
- `:active` - 点击激活状态
- `:focus` - 键盘焦点状态

### 响应式
- `@media (max-width: 640px)` - 移动端样式

## 设计 Token

### 颜色
```css
--bg-elevated: #ffffff           /* 按钮背景 */
--border-default: #e2e8f0        /* 默认边框 */
--color-primary: #3b82f6         /* 主色调（蓝色） */
--text-primary: #1e293b          /* 主文本色 */
```

### 间距
```css
--spacing-sm: 0.5rem             /* 8px */
--spacing-md: 1rem               /* 16px */
--spacing-lg: 1.5rem             /* 24px */
```

### 圆角
```css
--radius-md: 0.5rem              /* 8px */
```

### 过渡
```css
--transition-base: 200ms          /* 基础过渡时间 */
```

## Bootstrap Icons

使用 `bi-gear` 图标：
```html
<i className="bi bi-gear"></i>
```

确保项目中已引入 Bootstrap Icons：
```html
<link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.1/font/bootstrap-icons.css">
```

## 无障碍性

### ARIA 属性
```html
<button
  className="game-management-btn"
  onClick={...}
  aria-label="游戏管理"
>
  <i className="bi bi-gear"></i>
  <span>游戏管理</span>
</button>
```

### 键盘导航
- `Tab` - 聚焦按钮
- `Enter` / `Space` - 触发点击
- `Esc` - 关闭模态框

### 对比度
- 正常状态对比度：12.6:1 (AAA)
- Hover 状态对比度：5.8:1 (AA)

## 实现示例

### JSX 代码
```jsx
<button
  className="game-management-btn"
  onClick={(e) => {
    e.stopPropagation();
    setIsGameManagementOpen(true);
  }}
  aria-label="游戏管理"
>
  <i className="bi bi-gear"></i>
  <span>游戏管理</span>
</button>
```

### CSS 代码
```css
.game-management-btn {
  display: flex;
  align-items: center;
  gap: 0.5rem;
  padding: 0.5rem 1rem;
  background: #ffffff;
  border: 1px solid #e2e8f0;
  border-radius: 0.5rem;
  color: #1e293b;
  font-size: 0.875rem;
  font-weight: 500;
  cursor: pointer;
  transition: all 200ms;
  white-space: nowrap;
}

.game-management-btn:hover {
  background: #ffffff;
  border-color: #3b82f6;
  color: #3b82f6;
  transform: translateY(-1px);
  box-shadow: 0 2px 8px rgba(59, 130, 246, 0.15);
}

.game-management-btn:active {
  transform: translateY(0);
  box-shadow: 0 1px 4px rgba(59, 130, 246, 0.1);
}
```

## 测试检查清单

### 视觉测试
- [ ] 按钮显示在右下角
- [ ] 图标和文字正确显示
- [ ] 与其他按钮样式一致
- [ ] 移动端布局正确

### 交互测试
- [ ] Hover 效果正常
- [ ] Click 打开模态框
- [ ] 事件不冒泡
- [ ] 键盘导航可用

### 兼容性测试
- [ ] Chrome 浏览器
- [ ] Firefox 浏览器
- [ ] Safari 浏览器
- [ ] 移动端浏览器

---

**设计版本**：1.0.0
**创建日期**：2026-02-12
**设计系统**：Event2Table Design System

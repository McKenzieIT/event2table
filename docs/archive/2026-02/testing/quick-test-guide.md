# 快速测试指南

> **创建日期**: 2026-02-11
> **说明**: npm无法直接运行的临时解决方案

---

## 🚨 问题说明

执行 `npm run test:e2e` 时出现错误：
```
bash scripts/run-e2e-tests.sh: No such file or directory
```

**原因**: 可能是：
1. npm不在PATH中
2. 测试脚本路径问题
3. 需要使用完整路径

---

## ✅ 手动测试方法

### 方法1：使用完整路径

```bash
/Users/mckenzie/Documents/event2table/frontend/node_modules/.bin/npm run test:e2e
```

### 方法2：使用npx

```bash
npx npm run test:e2e
```

### 方法3：直接测试Playwright

```bash
cd /Users/mckenzie/Documents/event2table/frontend/tests/e2e
npx playwright test
```

---

## 📋 关键页面测试URL

### 1. Dashboard（首页）
```
http://localhost:5173/
或
http://localhost:5173/dashboard
```

**检查项**：
- [ ] 4个统计卡片显示
- [ ] 快速操作卡片可点击
- [ ] 最近游戏列表显示
- [ ] Badge显示正确

### 2. Games List（游戏列表）
```
http://localhost:5173/games
```

**检查项**：
- [ ] 搜索框可输入
- [ ] 游戏卡片显示
- [ ] checkbox可点击
- [ ] "新增游戏"按钮可点击
- [ ] Toast通知系统工作

### 3. Events List（事件列表）
```
http://localhost:5173/events
```

**前置条件**: 需先选择游戏

**检查项**：
- [ ] 事件列表显示
- [ ] 搜索框可输入
- [ ] 分类筛选可使用
- [ ] Badge显示正确
- [ ] Toast通知系统工作

### 4. Generate（HQL生成）
```
http://localhost:5173/generate
```

**检查项**：
- [ ] 游戏/事件选择正常
- [ ] "生成HQL"按钮可点击
- [ ] 生成后Toast通知显示

---

## 🎯 控制台命令检查

```bash
# 查看npm是否可找到
which npm

# 查看Vite是否可找到
which vite

# 启动开发服务器（如果npm可用）
cd /Users/mckenzie/Documents/event2table/frontend && npm run dev

# 如果都不可用，直接使用node
cd /Users/mckenzie/Documents/event2table/frontend && node node_modules/.bin/vite serve
```

---

## 🚨 临时建议

### 方案1：检查开发服务器是否已在后台运行

```bash
lsof -i :5173
```

如果有输出，说明服务器已经在运行。

### 方案2：检查端口占用

```bash
lsof -i :5173
```

### 方案3：直接在浏览器中打开URL测试

---

## ✅ 预期结果

**应该可以正常工作**：
- ✅ 所有页面加载无错误
- ✅ 所有导入路径已修复
- ✅ 所有Button变体已修复
- ✅ Toast通知系统工作
- ✅ 游戏上下文正确

---

**如果仍有问题**：

### 错误类型1: 导入错误
- **解决方案**: 检查 `@shared/ui` 别名配置

### 错误类型2: 组件API不匹配
- **解决方案**: 检查COMPONENT_SUMMARY.md中的API

### 错误类型3: Button变体错误
- **解决方案**: 使用 `variant="ghost"` 而非 `outline-secondary`

---

## 📝 提交测试报告模板

每次修改后，填写此报告：

```
测试日期: YYYY-MM-DD
测试人员: [您的名字]
代码修改: [描述修改的内容]

测试结果:
- Dashboard: ✅ / ❌ [备注]
- Games: ✅ / ❌ [备注]
- Events: ✅ / ❌ [备注]
- Generate: ✅ / ❌ [备注]
- Toast: ✅ / ❌ [备注]
- Button: ✅ / ❌ [备注]
- Other: ✅ / ❌ [备注]

发现的问题:
[列出具体问题]

修复措施:
[描述如何修复]

```

---

**现在请执行测试！**
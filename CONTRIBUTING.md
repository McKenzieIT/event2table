# 贡献指南

感谢你对Event2Table项目的关注！我们欢迎各种形式的贡献。

---

## 🤝 如何贡献

### 报告Bug

如果你发现了bug，请：
1. 检查[现有issues](../../issues)确保bug未被报告
2. 使用**Bug报告模板**创建新issue
3. 提供详细的复现步骤和环境信息

### 提出新功能

1. 检查[现有issues](../../issues)和[功能请求](../../labels/enhancement)
2. 使用**功能请求模板**创建issue
3. 说明功能的价值和使用场景
4. 考虑是否愿意帮助实现

### 改进文档

1. 找到需要改进的文档
2. 使用**文档改进模板**创建issue
3. 或直接提交PR（欢迎！）

### 提交代码变更

#### 工作流程

1. **Fork本仓库**
   ```bash
   # 在GitHub上点击Fork按钮
   ```

2. **克隆你的fork**
   ```bash
   git clone https://github.com/你的用户名/event2table.git
   cd event2table
   ```

3. **创建功能分支**
   ```bash
   git checkout -b feature/功能名称
   # 或
   git checkout -b fix/问题描述
   ```

4. **进行开发和提交**
   ```bash
   git add .
   git commit -m "类型: 简短描述"
   ```

   **提交信息规范**：
   - `feat:` 新功能
   - `fix:` Bug修复
   - `docs:` 文档更新
   - `style:` 代码格式（不影响功能）
   - `refactor:` 代码重构
   - `perf:` 性能优化
   - `test:` 测试相关
   - `chore:` 构建/工具链相关

5. **推送到你的fork**
   ```bash
   git push origin feature/功能名称
   ```

6. **创建Pull Request**
   - 访问：https://github.com/McKenzieIT/event2table
   - 点击"Compare & pull request"
   - 使用PR模板填写信息

---

## 📋 代码规范

### 开发规范

请遵守项目的开发规范，详见：
- **[CLAUDE.md](../CLAUDE.md)** - 核心开发规范（必读）
- **[docs/development/architecture.md](../docs/development/architecture.md)** - 架构设计
- **[docs/development/getting-started.md](../docs/development/getting-started.md)** - 快速开始

### TDD开发模式

**重要**：所有代码开发必须遵循测试驱动开发（TDD）：

1. ✅ 先写测试，看测试失败
2. ✅ 编写最小代码使测试通过
3. ✅ 重构优化，保持测试通过

详见：**/superpowers:test-driven-development** skill

### 游戏保护规则

**极其重要**：禁止修改或删除STAR001 (GID: 10000147)的数据

- ❌ 绝对禁止删除 GID 10000147 的游戏、事件、参数
- ✅ 所有测试使用 90000000+ 范围的测试GID
- ℹ️ 详见：[docs/development/STAR001-GAME-PROTECTION.md](../docs/development/STAR001-GAME-PROTECTION.md)

### 代码质量

- **Python**: 遵循PEP 8规范
- **TypeScript/JavaScript**: 遵循项目ESLint配置
- **SQL**: 使用参数化查询，禁止SQL注入
- **安全**: 输入验证、输出编码、错误处理

### API契约

- 所有API调用必须有后端实现
- HTTP方法必须匹配
- 参数格式一致（game_gid vs game_id）
- 运行：`python scripts/test/api_contract_test.py`

---

## 🧪 测试

### 运行测试

**后端测试**：
```bash
pytest backend/test/ -v
```

**前端测试**：
```bash
cd frontend
npm run test
```

**E2E测试**：
```bash
cd frontend
npm run test:e2e
```

### 测试覆盖率

- 新功能必须包含单元测试
- Bug修复应添加回归测试
- 目标：核心功能覆盖率 > 80%

---

## 📖 提交PR前检查

在提交PR之前，请确保：

- [ ] 代码通过所有测试
- [ ] 代码遵循项目规范（CLAUDE.md）
- [ ] 添加了必要的文档
- [ ] 更新了相关的README或指南
- [ ] 提交信息清晰（使用语义化提交）
- [ ] 没有合并冲突
- [ ] **游戏保护**：没有修改STAR001数据

---

## 👨‍⚖️ 代码审查流程

1. **提交PR**
   - 使用PR模板填写完整信息
   - 关联相关的issue

2. **自动检查**
   - CI/CD测试必须通过
   - 代码质量检查必须通过
   - API契约测试必须通过

3. **人工审查**
   - 至少一名维护者审查
   - 关注代码质量和安全性
   - 确保符合项目规范

4. **修改和反馈**
   - 根据反馈修改代码
   - 推送更新到功能分支
   - PR自动更新

5. **合并**
   - 审查通过后合并
   - 使用"Squash and merge"保持历史清洁
   - 删除功能分支

---

## 🎨 代码风格

### Python

```python
# ✅ 好的命名
def get_game_by_gid(game_gid: int) -> Dict[str, Any]:
    pass

class GameService:
    pass

# ❌ 避免
def getGameByGid(gameGid: int):
    pass
```

### TypeScript

```typescript
// ✅ 好的命名
const fetchGameByGid = (gameGid: number): Game => {
  // ...
};

interface GameData {
  gid: string;
  name: string;
}

// ❌ 避免
const fetch_game_by_gid = (game_gid: number) => {
  // ...
};
```

### SQL

```sql
-- ✅ 参数化查询
SELECT * FROM games WHERE gid = ?;

-- ❌ 避免（SQL注入风险）
SELECT * FROM games WHERE gid = '{gid}';
```

---

## 🔐 安全性

### 必须遵守

1. **SQL注入防护**
   - ✅ 使用参数化查询
   - ❌ 禁止字符串拼接SQL

2. **XSS防护**
   - ✅ 输入验证和输出编码
   - ✅ 使用Pydantic Schema验证

3. **错误处理**
   - ✅ 通用错误消息（不暴露内部信息）
   - ✅ 适当的HTTP状态码（400/404/409/500）

4. **敏感数据**
   - ✅ SECRET_KEY不提交到git
   - ✅ 使用.env文件管理环境变量
   - ✅ 数据库文件在data/目录

---

## 📧 联系方式

**项目维护者**：McKenzieIT
**Issue**：[GitHub Issues](https://github.com/McKenzieIT/event2table/issues)
**讨论**：[GitHub Discussions](https://github.com/McKenzieIT/event2table/discussions)

---

## 🎉 贡献者

感谢所有贡献者！你们的贡献使Event2Table变得更好。

---

**再次感谢你的贡献！** 🙏

---

## 📚 相关资源

- [项目开发规范](../CLAUDE.md)
- [架构设计文档](../docs/development/architecture.md)
- [GitHub设置指南](../docs/development/github-setup-guide.md)
- [API文档](../docs/api/README.md)
